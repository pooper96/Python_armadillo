from settings import Settings


class ProceduralAssets:
    """Placeholder for future caching of shapes, gradients, tiny PNGs if needed."""
    def __init__(self, settings: Settings):
        self.settings = settings
